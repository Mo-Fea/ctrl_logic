以下是我目前想的逻辑


开始：
进行init函数，完成后，按照如下双引号内字段进行打印

“这是R2完全测试脚本，ctrl+c可退出测试“
”请输入你想要进行的测试类型：“
“1.简单动作测试（只需要操控单个部件，或者移动，旋转测试）“
“2.复杂动作测试（多个部件联动，或者区域流程，或者完整流程）“

之后录入键盘输入，如果输入非预期值，打印“严格按照上述数字进行输入”

如果选择了1，则进入简单动作测试，按照以下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“进入简单动作测试，注意这里所有部件动作不会自动复位”
“如果需要恢复则重新进入简单测试，并且根据选项自己慢慢恢复”
“1.移动测试”
“2.旋转测试”
“3.吸盘头旋转测试”
“4.吸盘吸取状态测试”
“5.机械臂姿态测试”
“6.武器头夹爪开合测试”
“7.武器头拉起放下测试”
“0.返回最上级菜单”
之后录入键盘输入，如果输入非预期值，打印“严格按照上述数字进行输入”

简单测试下，选择1，进入移动测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“开始移动测试”
“注意，地图坐标系为，让机器正面面向梅林，机器面向的方向即为x轴正方向，x轴正方向逆时针旋转90度为y轴正方向，z轴数值向上”
“x轴逆时针旋转为正，顺时针旋转为负”
“请输入参考系(1为机器人参考系，2为武器头夹爪参考系)：”然后录入一个数字
“请输入目标x坐标：”  然后录入一个数字
“请输入目标y坐标：”  然后录入一个数字
“请输入最终机器朝向（-180.00到180.00，最好保留两位小数，注意0是关闭pid）”然后录入一个数字
“请输入机器移动最大速度（0-600）：”然后录入一个数字
“请输入机器到点判断阈值(单位m，至少保留两位):”然后录入一个数字

简单测试下，选择2，进入旋转测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“请输入旋转测试模式”
“1.细节旋转测试”
“2.简易旋转测试”
然后录入一个数字
如果进入细节旋转测试，则按原逻辑询问所有关键参数
如果进入简易旋转测试，则只询问目标角度，后续调用旋转中，分段阈值改为120度，判断阈值为1度


“开始旋转测试”
“注意，地图坐标系为，让机器正面面向梅林，机器面向的方向即为x轴正方向，x轴正方向逆时针旋转90度为y轴正方向，z轴数值向上”
“x轴逆时针旋转为正，顺时针旋转为负”
“请输入目标角度（-180.00到180.00，最好保留两位小数，注意0是关闭pid）”然后录入一个数字
"请输入大角度分段阈值（比如设置10,实际角度与目标角度相差大于10度时，会分成多个10度作为目标给下位机）：然后录入一个数字
“请输入完成判断阈值（保留两位小数，且大于0）：” 然后录入一个数字

简单测试下，选择3,进入吸盘头旋转测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“开始吸盘头旋转测试”
“当前吸盘头角度：”
“1对应ch9>700,2对应200<ch9<500,3对应-500<ch9<-300,4对应ch9<-700”
“请输入吸盘头朝向：” 然后录入一个数字

简单测试下，选择4，进入吸盘吸取状态测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“开始吸盘吸取状态测试”
“请输入控制气缸（0双气缸，1我约定为ch9>700时向前的那个气缸，2为另一个）：” 然后录入一个数字
“请输入吸取（1）或释放（2）：” 然后录入一个数字 

简单测试下，选择5，进入机械臂姿态测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“开始机械臂姿态测试”
"0原态，1高位吸取，2低位吸取，3.过渡态，4.放置态，5.侧吸态"
“请输入目标姿态：” 然后录入一个数字

简单测试下，选择6，进入武器头夹爪开合测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“开始武器头夹爪开合测试”
“请输入开（1）或合（0）:” 然后录入一个数字

简单测试下，选择7，进入武器头夹爪拉起放下测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“开始武器头夹爪拉起放下测试”
“请输入拉起（1）或放下（2）：” 然后录入一个数字

简单测试下，选择8，进入锁轮测试后，按照如下引号内字段打印：
“开始锁轮测试”
“请输入锁轮（1）或松开（2）：” 然后录入一个数字


------------------------------------------------------------------------------------------------------------
如果选择了2，则进入复杂动作测试，按照以下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“进入复杂动作测试”
“1.初始区域测试（未完成）”
“2.梅林区域测试（部分完成）”
“3.九宫格区域测试未完成”
“4.完整流程测试（未完成）”


-----------------------------------------------------------------------------------------------------------------------
复杂测试下，选择1,进入初始区域测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
"初始区域测试"
“逻辑为：先夹取目标武器头，然后后退并转向R1方向，锁轮等待”
“请输入目标武器头编号：”  然后录入一个数字
“请输入机器移动最大速度（0-600）：” 然后录入一个数字
之后将这两个录入数字作为参数输入fetch_weapon，并且执行

-----------------------------------------------------------------------------------------------------------------------
复杂测试下，选择2,进入梅林区域测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“梅林区域测试”
“此处所有动作只能逻辑连贯的执行”
“1.梅林动作测试准备（开始测试时，务必执行一次这个）”
“2.上下楼梯测试”
“3.方块吸取测试”
“4.侧吸测试”

-----------------------------------------------------------------------------------------------------------------------
梅林区域测试下，选择2,进入上下楼梯测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“上下楼梯测试”
“请输入当前台阶编号：” 然后录入一个数字
“请输入目标台阶编号：” 然后录入一个数字
之后将这两个数字融合进一个5列的动作行，上下楼梯标记为1,from_pos，to_pose为录入的输入，朝向根据这个判断
还要判断一下是否相邻，如果不相邻则打印“台阶不相邻，请输入正确的逻辑编号”

-----------------------------------------------------------------------------------------------------------------------
梅林区域测试下，选择3,进入方块吸取测试后，按照如下括号内字段打印：
“---------------------------------------------------------------------------------------------”
“方块吸取测试”
然后对当前台阶值做判断，与上下楼梯测试的询问逻辑一样
之后将当前台阶与目标台阶构建一个5列的动作行，上下楼梯标记为0,吸取方块标记为1
还要判断一下是否相邻，如果不相邻则打印“台阶不相邻，请输入正确的逻辑编号”

-----------------------------------------------------------------------------------------------------------------------
梅林区域测试下，选择4,进入侧吸测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“侧吸测试”
然后检查当前台阶是否为-1,不是-1则打印红色字段“非合法位置，请先移动到入口前的编号-1台阶”然后返回上一级菜单
如果是-1,则继续打印
“请输入要侧吸的方块所在台阶编号（1或3）：” 然后录入一个数字
接着将录入的数字作为to_pose,组合成（-1,to_pose,1,0,1）,作为行动作执行的输入进行执行

-----------------------------------------------------------------------------------------------------------------------
梅林区域测试下，选择5,进入边缘微调测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“边缘微调测试”
这里要读取当前台阶，如果不合法就打印“位置错误”，并返回上一级菜单
如果合法，就继续打印
“请输入微调方向（1：0 2：90 3：-90 4：180）：” 然后录入一个数字
“请输入微调距离(单位m，保留两位，0-0.5):”  然后录入一个数字
然后将这些作为输入调用微调函数，微调函数的高低参数判断设置为2

-----------------------------------------------------------------------------------------------------------------------
梅林区域测试下，选择6,进入完整测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
进入后先检测当前台阶是否为-1,如果不是，则打印"位置错误，请先做梅林动作准备"并返回上一级菜单
“完整梅林测试”
“请输入当前规则（1）挑战赛221（2）对抗赛331 ：”  然后录入一个数字
数字作为输入调用configure函数修改场内kfs数量。
接着打印“等待有效二维码输入中....”
然后开启二维码识别，完整路径规划线程，这里生成的矩阵应该会被推入一个queue，开启后等待0.5s，后续需要with这个二维码识别线程内的锁才能正常进行
获得线程锁之后，从queue中获取完整动作矩阵，调用执行完整动作矩阵方法，待完全执行完毕后，返回上一级菜单



复杂测试下，选择3,进入九宫格区域测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
"九宫格区域测试区域测试"
“1.放二层箱子测试”
“2.上R1测试（目前需要R2前端人为对准R1后进行）”
“3.R1上释放三层kfs测试”
“4.完整九宫格区域测试”


九宫格测试下，选择1，进入放二层箱子测试后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“二层箱子测试”
然后这里会检验当前suck_count,如果是1,则退出，并打印“目前没有吸取箱子”
若合规然后执行place_kfs_pose
再执行sucker_release_pose
然后以目标方向为0.01度移动到位置（4.00,0.95）
待移动到位置后，执行release_kfs


九宫格测试下，选择2，进入放上R1后，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“上R1测试”
“机器会面向九宫格方向，请将R1轨道对准R2”
“请回复1开始测试”  然后录入一个数字
开始后执行climb_R1

九宫格测试下，选择3，进入R1上释放三层kfs测试，按照如下引号内字段打印：
“---------------------------------------------------------------------------------------------”
“R1上释放三层kfs测试”
此处检验suck_count,需要大于等于二小于等于3,否则按照二层箱子测试处理
“不一定在R1上进行测试”
“此测试效果为持续检测下降，当下降高度大于4cm时释放kfs”
“请输入1开始测试：” 然后录入一个数字
开始后，先执行place_kfs_pose
再执行sucker_release_pose
最后执行release_3rd_kfs




R2 相差蓝半场逻辑//
侧吸没有完成//
路径规划需要优化//
逻辑 旋转cost+1 有R1+0.5 远距离出口+0.5//

 快速雷达切换
总体快速启动脚本
  内置快速重试

梅林动作优化//

偷大胜策略，稳分数策略//




我按你描述的主线对照了当前代码。结论是：`module.py` 已经有不少大块封装，但还没有一个完整的“比赛总流程”封装；九宫格前后的衔接还缺一层。

`module.init()`  
初始化通信、sender、flag 线程，并做初始机械状态：weapon 放下、夹爪打开、KFS/weapon 通道归安全值。

`module.fetch_weapon()`  
已经是初始区的大封装：  
移动到 weapon 前方 -> 移动到目标 weapon -> `weapon_seize()` 夹取 -> `weapon_up()` 抬起 -> 返回接近点 -> 旋转到释放方向。  
但它不负责二维码等待，也不负责释放 weapon。

`module.meilin_prepare()`  
移动到梅林入口逻辑点 `-1`，红蓝半场方向自动区分。它是“进入梅林前准备点”的封装。

`module.execute_action_matrix()`  
这是完整梅林动作矩阵执行器：逐行执行二维码规划出的动作矩阵，内部会调用 `execute_action_row()`，覆盖侧吸、KFS 吸取、上下楼，并处理下一行方向、上下楼连续跳过回中心等逻辑。
哦平锁
`module.high_score190()`  
已有高分策略封装，目前逻辑是：

```text
climb_R1_movement
-> climb_R1
-> place_3rd_kfs
-> sucker_release_pose
-> place_3rd_kfs
```

注意：它现在调用的是 `place_3rd_kfs()`，即 Z 下降检测释放，不会先调用新加的 `place_3rd_kfs_pose()`。

`module.totally_win()`  
已有大胜策略封装，目前逻辑是：

```text
move_to_2rd_place
-> release_kfs
-> climb_R1_movement
-> climb_R1
-> sucker_release_pose
-> place_3rd_kfs
```

同样，最后是 Z 下降检测释放，没有先调用 `place_3rd_kfs_pose()`。

**已有但不在 module.py 的关键封装**

`move.enter_battlefield()` 已经存在。逻辑是按当前红蓝半场读取：

```text
pre_entrance9_red/blue
-> entrance9_red/blue
```

并且全程保持 180 度方向码。  
但它在 `move.py`，目前没有被 `module.py` 的总流程调用。

`move.move_to_2rd_place()` 也在 `move.py`，供 `module.totally_win()` 使用。

**目前缺失的部分**

1. 缺一个真正的比赛总流程函数，比如 `module.run_competition()` 或 `module.complete_competition()`，把这些阶段串起来：

```text
fetch_weapon
-> 到指定点等待
-> QR 识别 / 获取梅林动作矩阵
-> 释放 weapon
-> meilin_prepare / 进入 -1
-> execute_action_matrix
-> 出梅林后进入 KFS 放置姿态
-> 同时/随后移动到 pre_entrance9
-> enter_battlefield
-> high_score190 或 totally_win
```

2. `module.py` 里没有 QR 识别等待和动作矩阵生成的封装。现在 QR 相关逻辑主要在 `ultimate_test_script.py` 里测试用，使用 `challenge_lib.start_background_qr_scanner()`，不属于 `module.py` 的比赛级封装。

3. “出梅林后机械臂进入放置 2 层或 3 层 KFS 状态，同时移动到九宫格 pre_entrance”这段目前没有整体封装。  
尤其是“同时”这一点目前没有看到并发封装；现有接口更偏顺序执行。

4. `move.enter_battlefield()` 已有，但 `module.py` 没有包装成九宫格入口阶段，也没有和放置姿态、梅林结束后的当前位置串联。

5. 九宫格完整测试在 `ultimate_test_script.py` 里还是未实现分支；README 也写了“九宫格完整测试”和“完整流程测试”未实现。

6. 新加的 `kfs.place_3rd_kfs_pose()` 目前还没有接入 `high_score190()` / `totally_win()`。如果你的新流程要求进入三层放置姿态，应当在对应策略里显式插入它，否则现在只是等待 Z 下降后释放。

整体看，现在代码已经有这些“积木”：抓 weapon、梅林准备、完整梅林矩阵、高分策略、大胜策略、进入九宫格。但缺的是把它们按比赛主线串起来的最外层调度函数，以及梅林出口到九宫格入口之间的机械臂姿态和移动衔接。




目前 [lib2/compete_logic.py](/home/mokuplus/ws_r2/lib2/compete_logic.py:1) 还是区域级封装，没有完整总入口。实现到这几块：

**已实现**
- `rigion_1(...)`  
  区域 1：启动后台 QR 扫描，同时抓 weapon。抓取成功后锁轮，等待 QR 扫描结束，再解锁、打开 weapon 夹爪并等待 `final_wait_sec`。返回里会带 `action_matrix`。

- `rigion_2(...)`  
  区域 2：从 `action_matrix_queue` 取动作矩阵，调用 `module.execute_action_matrix()` 执行完整梅林矩阵。

- `rigion_2_retry_plan(...)`  
  区域 2 手动重规划：输入当前 R1/R2 数量、还要抓几个 R2、当前 12 位场地字符串，然后生成新动作矩阵并推入队列。

- `rigion_3(...)`  
  区域 3：先 `move.enter_battlefield()` 进入九宫格，再按 `final_strategy` 执行：
  - `1`: `module.high_score190()`
  - `2`: `module.totally_win()`

**辅助函数**
- `_wait_until_lock_held(...)`
- `_read_int(...)`
- `_read_qr_string(...)`

**还没实现/还没串起来**
- 没有一个 `main` 或 `run_full_competition()` 把 `rigion_1 -> rigion_2 -> rigion_3` 自动串联。
- `competition_script.py` 仍没有接入这些区域函数。
- 函数名目前还是拼写成 `rigion_*`，不是 `region_*`。
- `rigion_1()` 自己创建了 `action_matrix_queue`，返回 `action_matrix`，但没有直接把同一个队列传给 `rigion_2()`。所以完整流程要么外部手动接结果，要么还需要改封装。






1.rigion 1 2 3
2.1区重试需要只需要完全重启
3.2区重试需要做到，手动输入场地情况，包括剩余R1,R2以及需要夹几个，然后继续执行rigion3
4.3区重试需要做到，启动后，开启两个气泵吸气，吸取两个箱子。然后根据获胜逻辑执行对应动作。注意要提前完成所有的机械臂动作。
5.挑战赛逻辑略有不同，需要做额外调整。
6.程序进入，需要询问红蓝半场，获胜逻辑，要抓的weapon


问题：
1.雷达快速切换
2.坐标问题
3.初始化问题
4.第三层kfs放置逻辑还没有修正
5.上R1改为侦察速度变化


----------------------------------------------------------------------------------------------------------
在competition_script.py中实现如下逻辑。
设置全局变量final_strategy，R1,R2，kfs_suck(该变量默认为2)，weapon_id，current_rule，meilin_situration（这个为12位梅林表示）
所有输入错误都按照打印“错误输入，请按照提示重新输入”来处理
所有非初级目录都要加一个”0.返回上一级“后续我不会标明
以下提到的大部分都已经有函数实现，尽量复用
在进入主运行时候按如下逻辑执行,带有双引号的是需要打印的文本：

“请输入当前红蓝半场（1.红 2.蓝）：” 录入一个数字，并根据这个数字调用红蓝半场的确认函数来确定对应逻辑
“请输入当前赛制(1.挑战赛 2.对抗赛)：” 录入一个数字，设置current_rule为这个数字，如果这里录入的是1，则设置全局变量R1,R2都为2，如果录入的是2，则全局变量R1,R2都设置为3
”请输入当前决定的获胜逻辑（1.大胜 2.190分）：“ 录入符合的数字 并修改final_strategy为对应数字

“---------------------------------------------------------”这条以及后续所有的分界线都是黄色的，可以为这个分界线的打印单独设置一个函数
“完整比赛脚本”
”1.完整比赛“
”2.区域重试“
”3.恢复至初态“
录入一个数字，然后进入对应分支

当选择1进入完整比赛后：
“---------------------------------------------------------”

  如果current_rule == 1则进入挑战赛：
  “---------------------------------------------------------”
  “请选择进行的项目（1.武林区域 2.九宫区域）：”
  录入一个数字，进入对应分支

      完整比赛，挑战赛，武林区域：
      “---------------------------------------------------------”
      “武林区域（挑战赛）”
      “请输入需要抓取的武器头编号（1-6）：”
      录入一个数字，并修改weapon_id为这个数字
      “请输入1开始执行：”

      完整比赛，挑战赛，九宫区域：
      “---------------------------------------------------------”
      “九宫区域（挑战赛）”
      “请输入1开始执行：”
      然后执行kfs_suck_preparation,参数为2
      接着询问"是否需要等待(0.否 1.是)："录入一个数字  如果为1则延时10s，2则直接跳过
      然后调用rigion_3
  
  如果current_rule == 2则进入对抗赛：
  “---------------------------------------------------------”
  “完整对抗逻辑”
  “请输入需要抓取的武器头编号（1-6）：”
  录入一个数字，并修改weapon_id为这个数字
  “请输入1开始执行：”

当选择2进入区域重试后：
“---------------------------------------------------------”
“区域重试“
”1.武馆区域重试“
“2.梅林区域重试”
“3.九宫格区域重试”
录入一个数字，进入对应分支

  当录入1，进入武馆区域重试时（逻辑上就是完整比赛启动）：
  根据current_rule，如果为1,则复用完整比赛/挑战赛/武林区域的逻辑，打印什么也都要
  如果为2,则直接复用完整比赛/对抗赛的逻辑

  当录入2,进入梅林区域重试的时候：
  “---------------------------------------------------------”
  “梅林重试”
  “请输入当前R1_KFS数量（0-3）：”录入一个数字，修改全局变量R1
  “请输入当前R2_KFS数量（0-3）：”录入一个数字，修改全局变量R2
  “请输入还需要吸取的R2_KFS数量（0-2）：” 录入一个数字，修改全局变量kfs_suck
  “请输入当前梅林情况：“ 录入一个12位字符串还是数字，修改全局变量meilin_situration
  “请输入1开始执行：”

  当录入3,进入九宫格区域重试：
  “---------------------------------------------------------”
  “九宫格区域重试”
  “请输入1开始执行：”
  “请输入需要吸取的KFS数量（0-2）：” 录入一个数字，修改全局变量kfs_suck


当选择3进入回复完全初始态后：
此处后续实现


先按照这个逻辑在打印上实现








该程序要实现如下菜单打印

“---------------------------------------------------------”
这是比赛快速启动脚本
1.完整比赛
1.1 挑战赛
1.2 对抗赛
2.区域重试
2.1 武馆区域重试
2.2 梅林区域重试
2.3 九宫格区域重试
3.恢复至完全初始态


现在加一个totally_win_challenge,输入中需要有一个数column（取值为1,2,3），一个数extra_needed(取0或者1)。逻辑上进入后，判断column值，然后先移动到对应的pre_column点，然后执行block_till_pressure.待退出block_till_pressure的阻塞之后，先延时2s，然后执行机械臂姿态方法place_kfs_pose，然后延时2s。之后移动到对应的column点，执行release_kfs。然后移动到pre_column2,接着对extra_needed做判断，如果为1,则置suck_count为3,并且气缸选择1（pf2），之后打开对应气缸，然后直接调用190_highscore_challenge.完成后返回。如果extra_needed为0,则移动到R1climb点，先后调用sucker_release_pose,与机械臂姿态place_3rd_kfs_pose，然后移动到R1climb点，后续逻辑与原来totally_win逻辑一样